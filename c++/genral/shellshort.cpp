#include<bits/stdc++.h>
using namespace std
void shell(int arr[],n){
  for(int gap=n/2;gap>0;gap/=2){
    for(int i = gap;i<n;++i){
        int j=i;
        int temp=arr[i];
        
    }
  }
}
 void main(){
    int n;
    cin>>n;
    int arr[n];
    for(inti=0;i<n;i++){
        cin>>arr[i];
    }
    display(arr,n);
    shell(arr,n);
    display(arr,n);
 }